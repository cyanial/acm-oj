#include <iostream>

using namespace std;

// 输入包括两个正整数a,b(1 <= a, b <= 1000),输入数据包括多组。
// 输出a+b的结果

int main() {
    int a, b;

    while (cin >> a >> b) {
        cout << a+b << endl;
    }

    return 0;
}